openapi: '3.1.0'
info:
  title: GreetingService
  version: 1.0.0
servers:
  - url: http://10.0.40.31:8080/api
    description: Rob-Work-M3 service
paths:
  /pjson:
    get:
      operationId: putJson
      parameters:
        - name: user
          required: false
          in: query
          description: The user of the topic.
          schema:
            type: string
        - name: topic
          required: false
          in: query
          description: The topic.
          schema:
            type: string
        - name: content
          required: false
          in: query
          description: The content of the topic.
          schema:
            type: string
      responses:
        '200':
          description: A success response with a greeting.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Json'
  /list:
    get:
      operationId: listTopics
      parameters:
      - name: user
        required: false
        in: query
        description: The owner of the topic.
        schema:
          type: string
      responses:
        '200':
          description: A success response.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Json'
  /gjson:
    get:
      operationId: getJson
      parameters:
      - name: user
        required: false
        in: query
        description: The owner of the topic.
        schema:
          type: string
      - name: topic
        required: false
        in: query
        description: The topic name.
        schema:
          type: string
      responses:
        '200':
          description: A success response.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Json'
components:
  schemas:
    Json:
      type: object
      description: A value with the greeting contents.
      properties:
        message:
          type: string
          description: The string representation of the greeting.
      required:
        - message
